export interface Project {
  image: string;
  title: string;
  description: string;
  link: string;
  tags: string[];
}

export interface SocialLink {
  icon: string;
  title: string;
  link: string;
}